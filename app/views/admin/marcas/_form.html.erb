<%= form_for(@admin_marca) do |f| %>
  <% if @admin_marca.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_marca.errors.count, "error") %> prohibited this admin_marca from being saved:</h2>

      <ul>
      <% @admin_marca.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
